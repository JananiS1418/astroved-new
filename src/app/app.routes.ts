import { Routes } from '@angular/router';
import { LiveAstrologyConsultation } from './pages/live-astrology-consultation/live-astrology-consultation';

export const routes: Routes = [
  { path: '', component: LiveAstrologyConsultation }
];

